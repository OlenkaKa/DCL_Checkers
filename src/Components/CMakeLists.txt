# Add all components here using ADD_COMPONENT(<COMPONENT_DIRECTORY>)
ADD_COMPONENT(CvInRange)

ADD_COMPONENT(ImageData)

ADD_COMPONENT(ColorPoints)

ADD_COMPONENT(ROSProxy)

ADD_COMPONENT(ImageSender)
